import { TokenService } from './token.service';
import { ResetCodeService } from './reset.service';

export const tokenService = new TokenService();
export const resetCodeService = new ResetCodeService();